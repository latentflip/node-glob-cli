{
  "name": "glob-cli",
  "version": "1.0.0",
  "bin": "./bin.js",
  "dependencies": {
    "globby": "^0.1.1"
  },
  "license": "ISC",
  "main": "bin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
